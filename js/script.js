/* ========================= typing animation ===========================*/
var typed = new Typed(".typing", {
    strings: [,"Delight", "High Quality","Chipwhiches", "Cookies", "Ice Cream"],
    typeSpeed: 100,
    BackSpeed: 60,
    loop:true
})

/* ========================= Smooth Scroll ===========================*/
var scroll = new SmoothScroll('a[href*="#"]', {
	speed: 2000,
	speedAsDuration: true
});
